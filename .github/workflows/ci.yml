name: CI - Model Training & Docker Build

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  train-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12.7'

      - name: Install Dependencies
        run: |
          pip install mlflow==2.19.0 pandas numpy scikit-learn matplotlib seaborn

      - name: Train Model with MLflow
        run: |
          cd MLProject
          mlflow run . --experiment-name="eccomerce-modelling" --env-manager=local

      - name: Upload Artifacts to GitHub
        uses: actions/upload-artifact@v4
        with:
          name: mlflow-artifacts
          path: MLProject/mlruns/

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      - name: Build Docker Image using MLflow
        run: |
          MODEL_PATH=$(cat MLProject/model_path.txt)
          mlflow models build-docker -m "MLProject/$MODEL_PATH" -n "${{ secrets.DOCKER_HUB_USERNAME }}/workflow-ci:latest" --env-manager=conda

      - name: Push Docker Image to Docker Hub
        run: |
          docker push ${{ secrets.DOCKER_HUB_USERNAME }}/workflow-ci:latest